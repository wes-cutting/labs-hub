---
id: 2026-07-26-lh-s1
type: status-report
status: Snapshot
roadmap-item: LH-S1
---

# Status Report — 2026-07-26 (#03)

| Field  | Value                                              |
| ------ | -------------------------------------------------- |
| Status | Snapshot                                           |
| Date   | 2026-07-26                                         |
| Author | wes-cutting + agent                                |
| Scope  | LH-S1 — hub foundation (Homepage + Portainer)      |

**Resume here:** The **hub foundation is built and running**. `deploy/compose.yml` defines the
hub (Homepage `:3000` + Portainer `:9000`); `make gate` is green (yamllint + `docker compose
config` + shellcheck) and CI runs the same on push/PR; the stack runs on the Pi from the
tracked definition and **auto-discovers** Jellyfin + Homebox from labels over a **read-only**
Docker socket; `make smoke` passes. Foundational choices: [ADR-0001/0002/0003]. **Next: LH-S2**
(Jellyfin as a first-class managed service, within the single-stream transcode budget). **Two
carries:** (1) git-deploy activation — mechanism built, needs the operator to push + make the
repo public + `git clone` on the Pi (currently staged via interim rsync at `~/labs-hub`); (2) a
visual browser confirmation of the tiles + resources widget, and creating the Portainer admin
on the fresh instance.

## 1. What landed since #02

| Item | Notes |
| ---- | ----- |
| `deploy/` foundation | `compose.yml` (Homepage RO-socket + Portainer RW-socket), Homepage config (docker discovery, resources widget), `.env.example`, `README.md` |
| Gate + CI | `Makefile` (`gate`/`up`/`down`/`smoke`/`deploy`), `scripts/smoke.sh`, `.yamllint`; `.github/workflows/gate.yml` wired (lint/validate/shellcheck) on push/PR |
| Spec | [FEAT-LH-S1](../features/LH-S1-hub-foundation.md) combined feature+UX, now `Implemented` |
| Running on Pi | Tracked stack up; ad-hoc eval containers replaced; Jellyfin/Homebox discovered (verified via Homepage `/api/services`) |

## 2. Definition of Done

| Check | State | Evidence |
| ----- | ----- | -------- |
| Usable end-to-end | ✅ | Hub up from `deploy/`; Homepage lists Jellyfin/Homebox with health; Portainer manages containers |
| Gate green (lint/validate/shellcheck) | ✅ | `make gate` exit 0 on the Pi; CI wired to same |
| Acceptance criteria met | ✅ | discovery via `/api/services`; smoke 200/200; RO socket confirmed (RW=false) |
| Docs updated in same change | ✅ | FEAT-LH-S1, roadmap + history, README, this report |
| Security (least-privilege / secrets) | ✅ | Homepage RO socket; repo scanned secret-clean before going public; `.env` gitignored |
| Accessibility | ⚠ | Third-party UIs; `statusStyle: basic` (text+icon, not colour-only). Full WCAG on Homepage/Portainer is outside our authorship — noted, not claimed |
| Visual acceptance (tiles/widget render) | ⚠ | API-verified; **operator to eyeball** the browser + create Portainer admin |
| Git-based deploy activation | ⚠ | Mechanism + docs done; **needs push + public repo + `git clone` on Pi** (interim rsync in place) |

## 3. Test totals

| Surface | Prev | Now | Δ |
| ------- | ---- | --- | - |
| Static gate (yamllint/compose/shellcheck) | 0 | 3 checks | +3 |
| Smoke (integration) | 0 | 1 script (2 probes) | +1 |
| Unit | 0 | 0 | 0 (no app code) |

## 4. Manual carries / deferred

| Item | Why | Owner / when |
| ---- | --- | ------------ |
| Push + make repo public + `git clone` on Pi | Activates git-based deploy (chosen: public repo) | wes-cutting — next |
| Visual confirm + Portainer admin | Browser-only checks; fresh Portainer instance | wes-cutting |
| Portainer socket-proxy | Narrow the RW-socket surface | `07_NFR` / hardening |
| Pin image versions/digests | Stricter reproducibility (currently `:latest`) | when settled |
| Config mount `:ro` | Tighten Homepage config bind (currently RW) | minor hardening |

## 5. Outstanding & next steps

1. **Activate git deploy:** push branch → merge → make repo public → on Pi `git clone https://github.com/wes-cutting/labs-hub.git ~/labs-hub` → `make deploy`.
2. **LH-S2** — Jellyfin as a managed service: bring it into a tracked Compose (data-root, labels), respect the single-stream transcode budget, add it to the hub. (Its direct-play vs transcode note is in the roadmap under LH-S2.)

## 6. Commands & gotchas (cold-start)

```sh
ssh wesker@192.168.1.185
cd ~/labs-hub && make gate        # yamllint + compose config + shellcheck
make up                           # bring the hub up (deploy/compose.yml)
make smoke HOST=localhost         # probe Homepage + Portainer
docker compose -f deploy/compose.yml ps
```
- Hub: Homepage `:3000`, Portainer `:9000`. Services discovered from `homepage.*` labels.
- `deploy/.env` (gitignored) holds `HOMEPAGE_ALLOWED_HOSTS` + `LABS_HUB_DATA_ROOT`.
- Data-root `/srv/labs-hub/portainer/data` (SD for now; SSD after SPIKE-02).
- Pi 5 has **no HW H.264 encode** → keep Jellyfin single-stream (LH-S2).
- `~/labs-hub` is currently an rsync copy; it becomes a real git clone at deploy activation.

## 7. Next-session kickoff prompt

```text
You are resuming labs-hub (baseline starter kit) in a fresh context window.
Get your bearings:
- Read CLAUDE.md and docs/00_WAYS_OF_WORKING.md.
- Read the NEWEST status report: docs/status-reports/2026-07-26-lh-s1.md (this one).
- Read docs/03_ROADMAP.md (LH-S2 is next) and docs/features/LH-S1-hub-foundation.md.

The hub foundation (LH-S1) is done and running on the Pi (Homepage + Portainer via
deploy/compose.yml; make gate green; CI wired). Next item: LH-S2 — bring Jellyfin in as a
first-class MANAGED service: define it in a tracked Compose (data-root per ADR-0002, homepage
labels for the catalog), respect the single-stream transcode budget (Pi 5 has no HW H.264
encode — favor direct-play; see the LH-S2 note in the roadmap), and confirm it appears healthy
in the hub. Write LH-S2's feature+UX spec first (right-size per §11). Watch out for: Pi at
192.168.1.185 (Wi-Fi, key SSH); repo now public (no secrets — keep it that way); Portainer RW
socket (socket-proxy is future hardening). Keep the slice vertical + gate-green; update docs in
the same change. Confirm the plan and risks before building.
```
