---
id: 2026-07-26-lh-s2
type: status-report
status: Snapshot
roadmap-item: LH-S2
---

# Status Report — 2026-07-26 (#04)

| Field  | Value                                          |
| ------ | ---------------------------------------------- |
| Status | Snapshot                                       |
| Date   | 2026-07-26                                     |
| Author | wes-cutting + agent                            |
| Scope  | LH-S2 — Jellyfin as a managed service          |

**Resume here:** **Jellyfin is now a tracked, managed hub service.** Defined in
`deploy/compose.media.yml` (composed with the hub via the Makefile's two `-f` files), it runs
on the Pi with config/cache on the `ADR-0002` data-root (`/srv/labs-hub/jellyfin/…`) and media
mounted **read-only** (`/srv/labs-hub/media`, seeded with the synthetic clip). `make gate` and
`make smoke` (now including Jellyfin `/health`) are green; Homepage discovers it. The ad-hoc
`spike01` Jellyfin is decommissioned.

> **Resume-context update (post-LH-S3 planning):** LH-S2 is **merged and the Pi clone is
> reconciled** (`git reset --hard origin/main && make deploy`, done). **LH-S3 is now `Blocked`,
> not the simple "custom-service pattern" this report first assumed:** the chosen first custom
> app, **budgeteer**, has **no auth** and holds **real financial data**, so it can't go on the
> LAN until it has its own auth (budgeteer roadmap #19, a cross-project prerequisite). Deploy
> shape decided for when unblocked: CI→GHCR ARM64 image + Postgres container, gated by
> budgeteer's own auth. See [`03_ROADMAP.md`](../03_ROADMAP.md) Current focus + the LH-S3 note.
> **Two viable next moves** (operator paused between them): (a) budgeteer's auth epic in its own
> repo, or (b) **SPIKE-02/03** here (SSD + at-rest encryption/backup, now more urgent with
> financial data heading to the Pi). **Still-open operator carry:** Jellyfin first-run (admin +
> `/media` library + set the simultaneous-stream cap to 1). Homebox still runs ad-hoc from
> `spike01` (formalize/retire later). **§5 + §7 below are updated to this reality.**

## 1. What landed since #03

| Item | Notes |
| ---- | ----- |
| `deploy/compose.media.yml` | Jellyfin: data-root config/cache, RO media, Homepage labels |
| Makefile / CI | `COMPOSE` spans hub + media (`-f compose.yml -f compose.media.yml`); CI validates both |
| `scripts/smoke.sh` | + Jellyfin `/health` check |
| `deploy/.env.example` / `deploy/README.md` | `LABS_HUB_MEDIA_ROOT`; media dirs + **transcode-budget** setup |
| Spec | [FEAT-LH-S2](../features/LH-S2-jellyfin.md) → `Implemented` |
| Pi | Ad-hoc Jellyfin removed; tracked Jellyfin up on the data-root; synthetic clip moved to `/srv/labs-hub/media` |

## 2. Definition of Done

| Check | State | Evidence |
| ----- | ----- | -------- |
| Usable end-to-end | ✅ | Tracked Jellyfin up from `deploy/`; discovered on the hub; reachable on `:8096` |
| Gate green | ✅ | `make gate` exit 0 (yamllint + compose config across both files + shellcheck); CI mirrors |
| Acceptance criteria met | ✅ | smoke 200/200/200; config+cache on data-root; media RO; tile discovered |
| Transcode budget addressed | ✅ (documented+config) / ⚠ (cap applied at first-run) | Budget documented in `deploy/README.md`; the per-user stream cap is set during Jellyfin first-run (operator) |
| Docs in same change | ✅ | FEAT-LH-S2, roadmap + history, README, this report |
| Security (least-privilege / secrets) | ✅ | Media mounted read-only; Jellyfin has its own auth; no secrets in repo |
| Accessibility | ⚠ | Jellyfin is a third-party UI (not authored here); hub tile inherits Homepage's a11y |
| Playback visual acceptance | ⚠ | `/health` green; **operator to first-run Jellyfin + play the clip + confirm the transcode cap** |

## 3. Test totals

| Surface | Prev | Now | Δ |
| ------- | ---- | --- | - |
| Static gate | 3 checks | 3 checks (compose now spans 2 files) | 0 |
| Smoke probes | 2 | 3 (+ Jellyfin) | +1 |

## 4. Manual carries / deferred

| Item | Why | Owner / when |
| ---- | --- | ------------ |
| Jellyfin first-run: admin + `/media` library + **stream/transcode cap** | Fresh instance; cap enforces the SPIKE-01 budget | wes-cutting — next visit |
| ~~Reconcile Pi clone after merge~~ | ~~LH-S2 files staged via interim rsync~~ | ✅ done (`git reset --hard origin/main && make deploy`) |
| Homebox still ad-hoc (`spike01`) | Out of LH-S2 scope | follow-up slice / retire |
| HW-accelerated HEVC decode (V4L2) | Widen the transcode budget | future hardening spike |
| Pin image versions; Portainer socket-proxy | Reproducibility / least-privilege | hardening |

## 5. Outstanding & next steps

1. **Operator carry:** Jellyfin first-run + set the per-user simultaneous-stream cap to 1 (SPIKE-01 budget); play the synthetic clip to confirm.
2. **LH-S3 is `Blocked`** (see [`03_ROADMAP.md`](../03_ROADMAP.md) Current focus + LH-S3 note). The first custom app, **budgeteer**, has no auth + real financial data → it needs its own auth before LAN exposure. Deploy shape decided: CI→GHCR ARM64 image + Postgres container, gated by budgeteer's own auth.
3. **The paused fork — pick one to resume:**
   - **(a) budgeteer auth epic** — in the *budgeteer* repo (its roadmap #19); the prerequisite that unblocks LH-S3.
   - **(b) SPIKE-02/03 here** — SSD + at-rest encryption/backup; now more urgent since budgeteer's financial data will land on the Pi, and it de-risks LH-S3's data handling.

## 6. Commands & gotchas (cold-start)

```sh
ssh wesker@192.168.1.185 && cd ~/labs-hub
make gate                         # yamllint + compose config (both files) + shellcheck
make up                           # hub + Jellyfin (compose.yml + compose.media.yml)
make smoke HOST=localhost         # Homepage + Portainer + Jellyfin /health
```
- Services: Homepage `:3000`, Portainer `:9000`, Jellyfin `:8096`. Data-root `/srv/labs-hub/{portainer,jellyfin,media}`.
- Compose is **multi-file** now (per-service `compose.*.yml`); the Makefile passes both `-f`.
- **Transcode budget:** Pi 5 has no HW H.264 encode — cap Jellyfin to 1 simultaneous stream; favor direct-play.
- Media mounted **read-only**; Jellyfin metadata lives in `/config` (data-root).

## 7. Next-session kickoff prompt

```text
You are resuming labs-hub (baseline starter kit) in a fresh context window.
Get your bearings:
- Read CLAUDE.md and docs/00_WAYS_OF_WORKING.md.
- Read the NEWEST status report: docs/status-reports/2026-07-26-lh-s2.md (this one) — esp. the
  "Resume-context update" note and §5.
- Read docs/03_ROADMAP.md (Current focus + the LH-S3 note).

Hub (LH-S1) + Jellyfin (LH-S2) are done and running on the Pi from tracked Compose
(deploy/compose.yml + deploy/compose.media.yml; make gate/smoke green; data on the data-root).

LH-S3 (deploy the budgeteer custom app) is BLOCKED: budgeteer has no auth + real financial
data, so it needs its own auth before LAN exposure. Do NOT deploy it until then. Decided
shape for when unblocked: CI→GHCR ARM64 image + Postgres container, gated by budgeteer's own
auth (formal ADR-0004 at build time).

Pick up ONE of (confirm with me first):
  (a) budgeteer auth epic — switch to the budgeteer repo (its roadmap #19); the prerequisite
      that unblocks LH-S3. Different project/context; use budgeteer's own kit process.
  (b) SPIKE-02/03 here — SSD medium + at-rest encryption/backup; now more urgent since
      budgeteer's financial data will land on the Pi. Write each spike report; time-box it.

Also open: operator's Jellyfin first-run (admin + /media library + simultaneous-stream cap=1).
Watch out for: Pi at 192.168.1.185 (Wi-Fi, key SSH); multi-file Compose convention; the
"sensitive custom services need default-deny auth before LAN exposure" rule. Keep slices
vertical + gate-green; update docs in the same change. Confirm the plan and risks before building.
```
