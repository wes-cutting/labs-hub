# ============================================================================
# CONFIDENTIAL / REAL DATA — NEVER COMMIT  (see docs/SECURITY.md, docs/00 §8)
# Real source/import data and exports stay local. Tests use synthetic fixtures.
# Drop confidential inputs in /data/ (gitignored) — not in the repo root.
# ============================================================================
/data/
/private/
/reports/
*.local
# Common confidential data file types kept out by default (allow-list a synthetic
# fixture explicitly with a negation if one must be committed, e.g. tests/fixtures/*).
*.xlsx
*.xls
*.csv
*.sqlite
*.db
*.dump
*.bak
!tests/**/fixtures/**

# ============================================================================
# SECRETS & CONFIG
# ============================================================================
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/

# ============================================================================
# DEPENDENCIES (multi-ecosystem; trim to your stack)
# ============================================================================
node_modules/
.pnpm-store/
vendor/
.venv/
venv/
__pycache__/
target/

# ============================================================================
# BUILD / OUTPUT
# ============================================================================
dist/
build/
out/
coverage/
*.tsbuildinfo
.next/
.cache/

# ============================================================================
# LOGS & TMP
# ============================================================================
*.log
tmp/
.tmp/

# ============================================================================
# OS / EDITOR
# ============================================================================
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
# Agent-spawned worktrees (K28): ESLint doesn't honor .gitignore, but a stale worktree left
# behind by a tool session shouldn't be tracked either way — and this line is the reminder.
**/worktrees/**

# ============================================================================
# HOMEPAGE runtime/generated config (LH-S1)
# Homepage auto-creates config files it's missing (e.g. kubernetes.yaml) and writes
# into its bind-mounted config dir. Track ONLY the files we manage so the git-based
# deploy clone's working tree stays clean.
# ============================================================================
deploy/homepage/config/*
!deploy/homepage/config/settings.yaml
!deploy/homepage/config/docker.yaml
!deploy/homepage/config/services.yaml
!deploy/homepage/config/widgets.yaml
!deploy/homepage/config/bookmarks.yaml
